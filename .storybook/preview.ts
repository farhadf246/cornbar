import "../src/styles.css";

const preview = {
  parameters: {
    backgrounds: {
      default: "light",
      values: [
        { name: "light", value: "#f3f4f6" },
        { name: "dark", value: "#111827" }
      ]
    }
  }
};

export default preview;
